# Debugging Notes

Add recurring, non-obvious debugging lessons.

When running Selective Trust Quarantine v2.2-RP, stop online integration if Gate B is `STOP_RANKER_NO_GAIN` because the protocol requires ranker-quality validation before controller LOSO/runtime claims.
